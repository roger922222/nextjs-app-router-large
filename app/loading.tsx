export default function RootLoading() {
  return (
    <div style={{ padding: "1rem" }}>
      <section>
        <h2>页面加载中...</h2>
        <p>这是一个根级加载占位（app/loading.tsx）。</p>
      </section>
    </div>
  );
}
